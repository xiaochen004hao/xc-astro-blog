name: Refresh Webmentions

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    name: Fetch and cache webmentions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Fetch webmentions
        run: npm run webmentions
        env:
          WEBMENTION_URL: ${{ secrets.WEBMENTION_URL }}

      - name: Commit updated cache
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # -f 强制添加被 .gitignore 忽略的 webmention 缓存文件
          # 这些文件是构建时 webmentions 显示的数据源，必须提交到仓库
          git add -f src/data/webmentions/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: refresh webmentions cache"
            git push
          fi
